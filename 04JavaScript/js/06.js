// Objeto Math

let resultado;

// Funciones

resultado = Math.PI;                    // Numero PI
resultado = Math.round(2.5);            // Redondea el numero
resultado = Math.ceil(2.1);             // Redondea hacia arriba siempre
resultado = Math.floor(2.9);            // Redondea hacia abajo siempre
resultado = Math.sqrt(144);             // Raiz cuadrada de un numero
resultado = Math.abs(-200);             // Convierte numeros negativos en positivos
resultado = Math.min(2, 5, 1, 8, 20);   // Muestra el numero mas chico
resultado = Math.min(2, 5, 1, 8, 20);   // Muestra el numero mas alto
resultado = Math.random();              // Convierte numeros negativos en positivos

// Convinar funciones

resultado = Math.floor(Math.random() *30 );            // Podemos convinar funciones