// Diferencias entre Metodos y Funciones

const numero1 = 20;
const numero2 = '20';

console.log( parseInt(numero2) ); // parseInt() es una funcino porque tiene el nombre seguido de unos parentecis y tiene un valor.
console.log( numero1.toString() ); // .toString() es un metodo porque empieza con un punto seguido del nombre de la funcion.